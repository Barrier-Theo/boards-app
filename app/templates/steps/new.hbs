<div>
  {{#ui-form newValue=model  validation="save" cancel="cancel" bigIcon='step forward' smallIcon='plus' header='Step' description='Adding an object'}}
    <div class="field">
      <label id="" class="" for="frmEdit-identity">Title</label>
      <div id="div-frmEdit-title" class="ui input">
        {{input id="frmEdit-title" value=model.title placeholder="title"}}
      </div>
    </div>
  {{/ui-form}}
</div>{{outlet}}